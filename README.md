# React + Vite

1. initiate a git clone
git clone https://github.com/Zibusiso-Ndlovu-Omni/Assignment-2.git

2. Change into the directory with the package.json file
cd assignment-2

3. Run a npm install
npm install

4. Run the application
npm run dev
